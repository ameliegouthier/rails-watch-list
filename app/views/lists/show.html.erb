<h1 class="mb-4 text-center fw-bold">🎬 <%= @list.name %></h1>

<div class="row g-4">
  <% @list.bookmarks.each do |bookmark| %>
    <div class="col-md-6 col-lg-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <h5 class="card-title">
            <%= bookmark.movie.title %>
            <span class="badge bg-warning text-dark ms-2"><%= bookmark.movie.rating %> ⭐</span>
          </h5>
          <p class="card-text text-muted"><%= bookmark.comment %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>

<div class="mt-4 d-flex justify-content-center gap-3">
  <%= link_to "⬅ Back to List", lists_path, class: 'btn btn-outline-secondary' %>
  <%= link_to "➕ Add a movie", new_list_bookmark_path(@list), class: 'btn btn-primary' %>
</div>
